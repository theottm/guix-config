(specifications->manifest
 (list
  ;; cli
  "sparql-query"

  ;; jupyter
  "jupyter"
  "graphviz"
  "python-sparqlkernel"

  ;; emacs
  "emacs-sparql-mode"

  ;; guile + jupyter
  ;; "guile-sparql" ;; fails
  "jupyter-guile-kernel"
  ))
